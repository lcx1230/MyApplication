<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".auth.AddPoetryActivity"
            android:exported="false" />
        <activity
            android:name=".home.ExitActivity"
            android:exported="false" />
        <activity
            android:name=".home.AppointmentActivity"
            android:exported="false" />
        <activity
            android:name=".auth.EditCounselorActivity"
            android:exported="true" />
        <activity
            android:name=".auth.AddCounselorActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt; -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".auth.CommunityManageActivity"
            android:exported="false" />
        <activity
            android:name=".auth.CounselorManageActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt; -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".auth.AdminActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt; -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".home.PsychologyTestActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".utils.TestActivity" -->
        <!-- android:exported="true"> -->
        <!-- &lt;!&ndash;            <intent-filter>&ndash;&gt; -->
        <!-- &lt;!&ndash;                <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt;&ndash;&gt; -->
        <!-- &lt;!&ndash;                <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt;&ndash;&gt; -->
        <!-- &lt;!&ndash;            </intent-filter>&ndash;&gt; -->
        <!-- </activity> -->
        <activity
            android:name=".home.HomeActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt; -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt; -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true"> <!-- 设置为 false，表示该 Activity 不能被其他应用启动 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- 设置为应用的入口点 -->
                <category android:name="android.intent.category.LAUNCHER" /> <!-- 设置为应用启动器中显示的 Activity -->
            </intent-filter>
            <!-- <meta-data -->
            <!-- android:name="android.app.lib_name" -->
            <!-- android:value="" /> -->
        </activity>
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true"> <!-- 设置为 true，表示该 Activity 可以被其他应用启动 -->
            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt; -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt; -->
            <!-- </intent-filter> -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".home.ProfileActivity"
            android:exported="true"> <!-- 设置为 true，表示该 Activity 可以被其他应用启动 -->
            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> &lt;!&ndash; 设置为应用的入口点 &ndash;&gt; -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> &lt;!&ndash; 设置为应用启动器中显示的 Activity &ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>